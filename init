#!/usr/bin/bash

tmp=`mktemp env_XXX`
echo saving old env files to $tmp.tgz
tar -czf $tmp.tgz .vimrc .bashrc .vim bash

dest=`realpath $0`
dest=${dest%/*}

cd
ln -sf $dest/vimrc .vimrc
ln -sf $dest/vim .vim
ln -sf $dest/bashrc .bashrc
ln -sf $dest/bash bash
