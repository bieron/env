[user]
	email = jbieron+github@gmail.com
	name = Jan Bieron

[alias]
    co = checkout
#    ci = commit
    s = status -sb
    di = diff --ignore-all-space
    f = fetch
    #l = log --pretty=format:'%Cred%h%Creset %C(yellow)%d%Creset %s %C(dim green)(%an, %cr)%Creset%b'
    l = log --pretty=format:'%Cred%h%Creset %C(yellow)%d%Creset %s %C(dim green)(%an, %cr)'
    lg = log --graph --pretty=format:'%Cred%h%Creset %C(yellow)%d%Creset %s %C(dim)(%an, %cr)'
    br = branch
    p  = pull --rebase
    pul = pull
    sss = stash

[core]
    editor = vim
    #whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol
	excludesfile = /home/jb/.gitignore
[push]
    default = simple
